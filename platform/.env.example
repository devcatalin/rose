# Copy to .env and replace with the output from `htpasswd -nbBC 12 admin "your-strong-pass"`
# The value must include the full `user:hash` string, for example: admin:$2y$12$...
LOGS_PASSWORD_HASH=admin:$2y$12$REPLACE_WITH_HASH
